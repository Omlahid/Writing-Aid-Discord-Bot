# .env.example
DISCORD_BOT_TOKEN=your-bot-token-here
LANGUAGE=en # Defaults to 'en'. Also supports 'fr' for French. Create your own translations by adding a new `<lang>.json` file in root and updating this variable. Use a valid IETF BCP 47 language tag or the code may not work properly.
TIMEZONE=America/Toronto # Defaults to 'America/Toronto'. Use a valid IANA time zone name or the code may not work properly. See https://en.wikipedia.org/wiki/List_of_tz_database_time_zones for a list of valid names.